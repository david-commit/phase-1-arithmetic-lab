const num1 = 31;
const num2 = 2;

const multiply = num1 * num2;

// Modulus expression
const num3 = 64;
const num4 = 6;

const mod = num3 % num4;

//Max Value
const max = Math.max(5, 6, 2, 20);

//Generates a random number
const random = Math.floor(Math.random() * 1000) + 1;

